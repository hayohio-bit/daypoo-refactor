{
  "name": "daypoo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@commitlint/cli": "^20.4.3",
    "@commitlint/config-conventional": "^20.4.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx,css}": [
      "frontend/node_modules/.bin/biome check --write --config-path frontend --no-errors-on-unmatched"
    ],
    "backend/**/*.java": [
      "bash -c 'cd backend && ./gradlew spotlessApply'"
    ]
  }
}
