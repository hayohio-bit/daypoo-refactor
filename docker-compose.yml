version: '3.8'

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: daypoo_postgres
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      # 로컬 고정 포트. .env 의 DB_PORT 가 있으면 그 값이 우선한다.
      - "${DB_PORT:-15432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: daypoo_redis
    ports:
      # 로컬 고정 포트. .env 의 REDIS_PORT 가 있으면 그 값이 우선한다.
      - "${REDIS_PORT:-16379}:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
